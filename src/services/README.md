Services folder: API clients and external integrations.

Suggested files:
- `api.ts` (axios/fetch wrapper)
- `authService.ts` (Cognito/OIDC wrappers)
- `qrcodeService.ts`

Keep side-effects and networking code here.